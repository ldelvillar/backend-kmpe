{
  "name": "backend-kmpe",
  "version": "1.0.0",
  "description": "Backend service for KMPE landing page",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && node -e \"require('node:fs').cpSync('src/data', 'dist/data', { recursive: true })\"",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "author": "ldelvillar",
  "license": "ISC",
  "dependencies": {
    "@mistralai/mistralai": "^1.10.0",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
